@smoke
  Feature: user could hover in the main category and select sub category
    Scenario: user hover to main category and randomly select sub category
      When user select sub category
      Then sub category page open
